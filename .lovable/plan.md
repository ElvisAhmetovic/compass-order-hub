

## Replace "Empria Dental" with "AB Media Team" in All Email Templates

### What changes
Every visible mention of "Empria Dental" / "Empria" / "EMPRIA Dental CRM" in email HTML content will be replaced with "AB Media Team". The `from` address (`noreply@empriadental.de`) and URLs stay unchanged since those are infrastructure/domain settings.

### Files to update (5 edge functions + 1 component)

| File | What to change |
|------|---------------|
| `supabase/functions/send-order-confirmation/index.ts` | Header `<h1>Empria Dental</h1>` → `AB Media Team`. Footer "generated by Empria Dental" → "generated by AB Media Team". Footer link text `empriadental.de` → `AB Media Team` |
| `supabase/functions/send-order-payment-reminders/index.ts` | Footer "EMPRIA Dental CRM" → "AB Media Team" |
| `supabase/functions/create-client-ticket/index.ts` | Footer "automated notification from Empria." → "automated notification from AB Media Team." |
| `supabase/functions/send-client-portal-credentials/index.ts` | Check for any visible "Empria" branding in email HTML |
| `supabase/functions/send-client-invite/index.ts` | Check for any visible "Empria" branding in email HTML |
| `src/components/notifications/TemporaryNotificationBanner.tsx` | Update Croatian text references to Empria/EmpriaDental → AB Media Team (if still in use) |

All 5 modified edge functions will need redeployment. Text-only changes, no logic changes.

