

## Remove Duplicate Close Button from Order Modal

The modal has two X buttons: one custom button in the header (line ~129) and one auto-generated by Radix's `DialogContent` component (in `dialog.tsx`). 

### Fix

**`src/components/dashboard/OrderModal.tsx`** — Remove the custom X button next to the edit button (around line 128-130):

```tsx
// Remove this:
<Button variant="ghost" size="icon" onClick={handleModalClose}>
  <X className="h-4 w-4" />
</Button>
```

Keep the Radix built-in close button (top-right corner) since it's standard and handles accessibility correctly.

